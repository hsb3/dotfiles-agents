# GitHub issues are BUG-REPORT INTAKE ONLY (decision-1, 2026-08-03).
# Planned work, features, and epics live on the Kaneo board (project DFA) —
# a reported bug gets a board task when it is planned, and the issue is
# closed by hand after the fix merges into dev (auto-close only fires on main).
blank_issues_enabled: false
