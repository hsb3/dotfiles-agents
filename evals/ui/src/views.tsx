import { Breadcrumb, BreadcrumbItem, Button, Column, Grid, Header, HeaderName, SideNav, SideNavItems, SideNavLink, SkipToContent } from "@carbon/react";
import Content from "@carbon/react/es/components/UIShell/Content";
import { useEffect, useState, type ReactNode } from "react";
import { Session } from "./api";
import { Catalog } from "./catalog";
import { Documentation } from "./documentation";
import { Evaluations } from "./evaluations";
import { Block, Login } from "./ui";

type Route = "home" | "catalog" | "documentation" | "performance" | "evaluations";
const routes: { id: Route; label: string }[] = [{ id: "home", label: "Home" }, { id: "catalog", label: "Catalog" }, { id: "documentation", label: "Documentation" }, { id: "performance", label: "Performance" }, { id: "evaluations", label: "Evaluations" }];
const currentRoute = (): Route => routes.some(({ id }) => location.hash === `#${id}`) ? location.hash.slice(1) as Route : "home";
function Crumbs({ route }: { route: Route }) { return <Breadcrumb noTrailingSlash><BreadcrumbItem href="#home">Home</BreadcrumbItem>{route !== "home" && <BreadcrumbItem isCurrentPage>{routes.find((item) => item.id === route)?.label}</BreadcrumbItem>}</Breadcrumb>; }
function Shell({ route, children, logout }: { route: Route; children: ReactNode; logout: () => void }) { return <><Header aria-label="Toolbox"><SkipToContent href="#main-content" /><HeaderName href="#home" prefix="">Toolbox</HeaderName><span className="header-caption">Source and evidence workspace</span><Button kind="ghost" size="sm" className="logout" onClick={logout}>Log out</Button></Header><div className="shell"><SideNav isFixedNav expanded aria-label="Toolbox navigation" className="sidebar"><SideNavItems>{routes.map((item) => <SideNavLink key={item.id} href={`#${item.id}`} isActive={route === item.id} aria-current={route === item.id ? "page" : undefined}>{item.label}</SideNavLink>)}</SideNavItems></SideNav><Content id="main-content" tabIndex={-1} className="content">{children}</Content></div></>; }
const homeAreas = [["catalog", "Catalog", "Catalog discovers packaged workflows/plugins and database primitives."], ["documentation", "Documentation", "Documentation reads stored instructions, references, and source context."], ["performance", "Performance", "Performance explains when measurement provenance and comparability become available."], ["evaluations", "Evaluations", "Evaluations inspects current assessments and per-job coverage."]] as const;
export function Home() { return <><Block><Crumbs route="home" /><h1>Orient to the Toolbox</h1><p className="lede">Choose a source-aware area. Records remain separate unless the database explicitly relates them.</p></Block><Grid fullWidth className="area-grid">{homeAreas.map(([id, label, text]) => <Column key={id} sm={4} md={4} lg={4}><article className="area-card"><h2>{label}</h2><p>{text}</p><Button kind="tertiary" href={`#${id}`}>Open {label}</Button></article></Column>)}</Grid></>; }
function Performance() { return <Block><Crumbs route="performance" /><h1>Performance</h1><section className="surface"><h2>Measurement availability</h2><p>Performance becomes comparable only when provenance, availability, and validity are explicit. This workspace will show measurement records when that contract is present.</p><Button kind="tertiary" href="#home">Return to Home</Button></section></Block>; }
export function BrowserApp() { const [session] = useState(() => new Session()); const [signedIn, setSignedIn] = useState(false); const [route, setRoute] = useState<Route>(currentRoute()); const clearUi = () => { setSignedIn(false); setRoute("home"); }; const logout = () => { session.logout(); clearUi(); }; useEffect(() => { const unsubscribe = session.subscribe(clearUi); return () => { unsubscribe(); }; }, [session]); useEffect(() => { const change = () => setRoute(currentRoute()); addEventListener("hashchange", change); return () => removeEventListener("hashchange", change); }, []); useEffect(() => { document.getElementById("main-content")?.focus(); }, [route]); if (!signedIn) return <Login session={session} onAuthenticated={() => setSignedIn(true)} />; const content = route === "home" ? <Home /> : route === "catalog" ? <Catalog session={session} onExpired={clearUi} /> : route === "documentation" ? <Documentation session={session} onExpired={clearUi} /> : route === "evaluations" ? <Evaluations session={session} onExpired={clearUi} /> : <Performance />; return <Shell route={route} logout={logout}>{content}</Shell>; }
