import { Button, Column, Grid, Pagination, Search } from "@carbon/react";
import { useEffect, useState } from "react";
import { Session } from "./api";
import { catalog, distributions, extenders, filterCatalog, type Catalog as Snapshot, type Extender } from "./data";
import { Block, PageCrumbs, StateNotice } from "./ui";
type State = "loading" | "access" | "error" | "empty" | "populated";
const expired = (r: { kind: string; status?: number }, done: () => void) => { if (r.kind === "access" && r.status === 401) done(); };
function Database({ session, query, onExpired }: { session: Session; query: string; onExpired: () => void }) { const [state, setState] = useState<State>("loading"), [rows, setRows] = useState<Extender[]>([]), [page, setPage] = useState(1), [total, setTotal] = useState(0), [dState, setDState] = useState<State>("loading"), [members, setMembers] = useState<{ id: string; slug: string; members: string[] }[]>([]); useEffect(() => { const c = new AbortController(); extenders(session, query, page, c.signal).then((r) => { if (c.signal.aborted) return; if (r.kind === "ok") { setRows(r.data.items); setTotal(r.data.totalItems); setState(r.data.items.length ? "populated" : "empty"); } else { setState(r.kind); expired(r, onExpired); } }); return () => c.abort(); }, [onExpired, page, query, session]); useEffect(() => { const c = new AbortController(); distributions(session, c.signal).then((r) => { if (c.signal.aborted) return; if (r.kind === "ok") { setMembers(r.data.items); setDState(r.data.items.length ? "populated" : "empty"); } else { setDState(r.kind); expired(r, onExpired); } }); return () => c.abort(); }, [onExpired, session]); return <section className="surface"><h2>Database extenders</h2>{state === "populated" ? <><ul>{rows.map((row) => <li key={row.id}>{row.name || row.slug} · {row.kind}</li>)}</ul><Pagination page={page} pageSize={25} pageSizes={[25]} totalItems={total} onChange={({ page }) => setPage(page)} /></> : <StateNotice state={state} subject="extender" />}<h3>Explicit distribution membership</h3>{dState === "populated" ? <ul>{members.map((row) => <li key={row.id}>{row.slug} · {row.members.length} member record(s)</li>)}</ul> : <StateNotice state={dState} subject="distribution" />}</section>; }
export function Catalog({ session, onExpired }: { session: Session; onExpired: () => void }) { const [state, setState] = useState<State>("loading"), [snapshot, setSnapshot] = useState<Snapshot>(), [query, setQuery] = useState(""), [page, setPage] = useState(1); useEffect(() => { const c = new AbortController(); catalog(session, c.signal).then((r) => { if (c.signal.aborted) return; if (r.kind === "ok") { setSnapshot(r.data); setState("populated"); } else { setState(r.kind); expired(r, onExpired); } }); return () => c.abort(); }, [onExpired, session]); const combined = [...filterCatalog(snapshot?.workflows ?? [], query).map((row) => ({ ...row, family: "Workflow" })), ...filterCatalog(snapshot?.plugins ?? [], query).map((row) => ({ ...row, family: "Plugin" }))], shown = combined.slice((page - 1) * 10, page * 10); return <><Block><PageCrumbs current="Catalog" /><h1>Catalog</h1><p>{snapshot?.source_snapshot ? `Packaged source snapshot: ${snapshot.source_snapshot}.` : "Packaged records are separate from database extenders and explicit distribution membership."}</p><Search id="catalog-search" labelText="Search packaged snapshot" value={query} onChange={(e) => { setQuery(e.currentTarget.value); setPage(1); }} /><Button kind="ghost" onClick={() => { setQuery(""); setPage(1); }}>Reset search</Button></Block>{state !== "populated" ? <Block><StateNotice state={state} subject="catalog" /></Block> : <><Grid fullWidth className="area-grid"><Column sm={4} md={8} lg={8}><section className="surface"><h2>Packaged catalog</h2>{shown.map((row, i) => <article key={row.id ?? i}><p>{row.family}</p><h3>{row.name ?? row.slug ?? row.id}</h3><p>{row.guidance ?? row.description}</p></article>)}{!combined.length && <p>No packaged records match this search.</p>}</section></Column><Column sm={4} md={8} lg={8}><Database session={session} query={query} onExpired={onExpired} /></Column></Grid><Block><Pagination page={page} pageSize={10} pageSizes={[10]} totalItems={combined.length} onChange={({ page }) => setPage(page)} /></Block></>}</>; }
