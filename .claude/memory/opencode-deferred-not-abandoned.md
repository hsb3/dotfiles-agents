---
name: opencode-deferred-not-abandoned
description: opencode support is a live promise the owner deferred, not dropped — do not strip the opencode targets or generator
metadata:
  type: project
---

This marketplace is Claude-Code-only today, but opencode support is **deferred, not abandoned** (owner, 2026-07-20). The roster's `targets: [claude-code, opencode]`, `scripts/gen_opencode.py`, `docs/atelier-parity.md`, and the `opencode-expertise` skill exist to keep that promise; the open card is 63nm (bring the opencode plugin targets in, atelier first, owner-gated questions first). **How to apply:** do not propose removing opencode targets or the generator as cleanup, and expect the root `plugins/` layout to need revisiting when opencode lands.
