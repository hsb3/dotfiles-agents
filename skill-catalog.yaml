# skill-catalog.yaml — THE STANDALONE SKILL CATALOG
#
# The set of skills that qualify to ship *standalone* — one skill installed on its own,
# without taking a whole plugin bundle. Claude Code gets a generated one-skill marketplace
# plugin per entry (scripts/gen_standalone.py, wired into `make build`). The skill BODY still
# lives once in primitives-core/skills/<id>/ — this catalog is a thin projection that only
# references roster ids and adds the standalone-specific fields; it never re-authors source,
# provenance, or `requires`, which stay owned by the roster (primitives-core.yaml).
# scripts/check_skill_catalog.py enforces eligibility + catalog<->roster drift + the
# namespacing invariant, and runs in `make ci`.
#
# Claude-Code-only distribution (R4): `clients` carries `claude-code` alone.
#
# Entry schema (thin projection — everything else is read from the roster):
#   id                 the roster skill id this entry projects (must be type: skill)
#   standalone         true — installable on its own (the only value today)
#   clients            [claude-code] — must be a subset of the roster targets[]
#   depends_on_skills  [] — asserted empty; a non-empty value disqualifies (no sibling coupling)
#   prerequisites      advisory cli:/env: needs the installer PRINTS (never rejects)
#   provenance         must equal the roster `origin` for this id (drift-checked)
#
# D4 seeded the mechanism with `private-fork` — a skill that ships BOTH in a bundle and as a
# one-skill install, proving the standalone mechanism coexists with bundles. Of the five
# originally standalone-only candidates, the R2-remainder ruling shipped `opencode-expertise`
# only; `carbon-builder`, `notion-api`, `dlt-pipelines`, and `subagent-creator` do not ship in
# this rebuild (deferred / superseded — see the epic tracking issue, not a permanent
# retirement). `github-project-board` joined the catalog at E4 (bundle recomposition, ruling
# 0020) — it ships standalone-only (no bundle membership).

version: 1

skills:
  - id: private-fork
    standalone: true
    clients: [claude-code]
    depends_on_skills: []
    prerequisites: []
    provenance: authored
  - id: opencode-expertise
    standalone: true
    clients: [claude-code]
    depends_on_skills: []
    prerequisites: []
    provenance: authored
  - id: pptx-themes
    standalone: true
    clients: [claude-code]
    depends_on_skills: []
    prerequisites: []
    provenance: authored
  - id: github-project-board
    standalone: true
    clients: [claude-code]
    depends_on_skills: []
    prerequisites: []
    provenance: authored
  - id: owner-signoff
    standalone: true
    clients: [claude-code]
    depends_on_skills: []
    prerequisites: []
    provenance: authored
  - id: deep-research
    standalone: true
    clients: [claude-code]
    depends_on_skills: []
    prerequisites: []
    provenance: authored
  - id: dataviz
    standalone: true
    clients: [claude-code]
    depends_on_skills: []
    prerequisites: []
    provenance: authored
  - id: update-config
    standalone: true
    clients: [claude-code]
    depends_on_skills: []
    prerequisites: []
    provenance: authored
  - id: project-memory
    standalone: true
    clients: [claude-code]
    depends_on_skills: []
    prerequisites: []
    provenance: authored
