# _meta — local working desk

Tracked by default (ADR-0006); only `operations/` content, tool caches, and OS litter are ignored.

- `operations/` — live URLs, credentials, runbooks with secrets. **Never tracked.**
- `research/`, `briefings/` — tracked; clone-survivable by default.

Project decision record lives outside the repo at
`~/Documents/Claude/Projects/dotfiles-agents-cowork/_structure/` (`CANON.md`).
