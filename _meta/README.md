# _meta — local working desk

Gitignored by default; durable items tracked via gitignore negation (this file, `HANDOFF.md`).

- `operations/` — live URLs, credentials, runbooks with secrets. **Never tracked.**
- `research/`, `briefings/` — as needed.

Project decision record lives outside the repo at
`~/Documents/Claude/Projects/dotfiles-agents-cowork/planning/` (`CANON.md`).
