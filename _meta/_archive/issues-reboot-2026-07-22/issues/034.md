# #34: Standards fix: scaffolded _meta/ .gitkeeps don't survive clone (gitignore vs scaffold contradiction)

- **State:** CLOSED
- **Labels:** type:fix
- **Created:** 2026-07-02T12:47:07Z
- **Closed:** 2026-07-02T18:23:39Z
- **URL:** https://github.com/hsb3/dotfiles-agents/issues/34

## Body

Found while dogfooding conformance: the scaffold promises "empty dirs get a .gitkeep so they survive clone", but `_meta/*` is gitignored and the standard's own gitignore template negates only plans/HANDOFF/README/mise-en-place — so `_meta/{archive,briefings,operations,research}/.gitkeep` are IGNORED. META-01/02/04/05 pass on the working copy but re-GAP on every fresh clone (verified via git check-ignore on both host repos).

## Deliverables
- Pick + implement one: (a) negate the .gitkeeps in the gitignore template (operations/ arguably stays untracked by design), (b) audit treats gitignored-empty dirs as locally-satisfiable and says so, or (c) scaffold stops gitkeeping ignored dirs and the checklist rows document working-tree-only semantics.

## Acceptance criteria
- Fresh-clone audit of a conformant repo shows no META re-gaps (or the rows explicitly state working-tree semantics).
- scaffold/audit/gitignore-template no longer contradict; drift-guarded where possible.

Refs: conformance run reports (2026-07-02), both builders flagged independently.

## Comments (0)

(no comments)