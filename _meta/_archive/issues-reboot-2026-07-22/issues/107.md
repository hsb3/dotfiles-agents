# #107: feat: promote context-watermark + handoff-freshness hooks into primitives-core (parked — rides the repo rewrite)

- **State:** CLOSED
- **Labels:** (none)
- **Created:** 2026-07-14T06:35:17Z
- **Closed:** 2026-07-16T23:04:13Z
- **URL:** https://github.com/hsb3/dotfiles-agents/issues/107

## Body

> **Tracking:** lineup ruling 2026-07-14 (executive-desk decision 0006, amendment 1).
> **PARKED — rides the one-repo-rewrite; do not start standalone.** Filed now so the
> rewrite's work order carries it and the candidates don't stay losable machine-local
> files meanwhile.

Promote the two highest-firing extenders in the census into primitives-core as
project-workflow's enforcement ("nudge") layer, per packaging requirement P4:

- **context-watermark** — 80 firings in its first 3 days
- **handoff-freshness-guard** — 63 firings, same window

Both currently live as machine-local, unmanaged hook files in the fable-optimization
project — undistributable, unversioned, losable. This is promote-and-harden of things
already running daily (buy-before-build G3), not a build.

## Deliverables

- Both hooks through the workbench promotion gate into primitives-core as roster hook
  primitives (promotion-log records per the lifecycle).
- Packaged into project-workflow as its nudge layer (P4: agent + hook per plugin).
- Machine-local originals replaced by the distributed copies.

## Acceptance criteria

- [ ] Rendered Claude Code output is behavior-identical to the fable-optimization
      originals (side-by-side run on one session).
- [ ] Roster entries carry `origin: authored` with promotion-log cross-refs.
- [ ] The fable-optimization machine-local copies are retired in favor of the
      distributed ones.

## Sequencing

Blocked on the repo rewrite starting (provenance moves + repackage). If the rewrite's
scope is cut, this returns to the board as a standalone candidate — with a fresh
usage window as evidence (decision 0006 amendment 2).


## Comments (2)

### Comment by @hsb3 at 2026-07-14T08:37:38Z

**Reconciliation — the target bundle lineup is now ruled (executive-desk `decisions/0007`, 2026-07-14).**

0007 splits the old `project-workflow` plugin into two shipping bundles (Fork B): the **loop** (which *keeps the `project-workflow` name* — handoff, planning-desk, comms, github-project-board, board-triage) and a new **repo-standards** bundle (compliance-audit, mise-en-place, memory-taxonomy, repo-meta-structure, readme-value-and-proof, private-fork).

**Effect on this issue — the target is confirmed, lightly sharpened:**
- Both hooks attach to the **loop bundle** (`project-workflow`, which retains the name), as its nudge layer — not the repo-standards bundle. context-watermark (context management) and handoff-freshness-guard (handoff nudge) are both loop concerns, so the P4 "agent + hook per plugin" framing lands on the loop bundle.
- The deliverables and acceptance criteria are otherwise unchanged: promote-and-harden through the workbench gate, roster hook primitives with promotion-log records, machine-local originals retired, behavior-identical side-by-side.

Still **parked, rides the one-repo-rewrite** — 0007 fixes the marketplace target the rewrite builds toward, so the rewrite's work order can now carry this cleanly.

### Comment by @hsb3 at 2026-07-16T23:04:13Z

**Executive-desk re-triage ruling (Henry, 2026-07-16; enacting decision 0016 §6 "keep only what a use case still pulls"; recorded as desk decision 0017).**

**Closed — superseded by the candidate path.** The two named hooks (context-watermark, handoff-freshness) now ship in the foreman-kit candidate, which enters via the new distribution entry gate; the broader which-hooks-to-carry question is the ledger's deferred 10-hook evaluate-or-retire item (revisited when the hook set is next touched). The gate's machine floor already accepts the ratified hook-dir layout.
