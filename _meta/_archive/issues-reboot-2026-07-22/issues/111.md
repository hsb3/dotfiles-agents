# #111: epic: one-repo-rewrite — rebuild source to the target bundle lineup (0007)

- **State:** CLOSED
- **Labels:** area:plugins, epic
- **Created:** 2026-07-14T08:44:00Z
- **Closed:** 2026-07-20T13:01:18Z
- **URL:** https://github.com/hsb3/dotfiles-agents/issues/111

## Body

> **Tracking:** executive-desk decisions `0007` (target lineup) · `0016` (fresh start — reframes this epic) · `0017` (re-triage). Body updated 2026-07-16 per 0016: the vehicle is a **clean-room rebuild** (same repo name/remote, **fresh git history**), not an in-place rewrite. The prior in-place body is preserved in this issue's edit history; its content is absorbed below.

## The shape (what the rebuilt repo is)

Rebuild `dotfiles-agents` clean-room to the 0007 lineup: same name/remote (protects the name-keyed marketplace ref — verified 2026-07-16), fresh history. `dev` = source, `main` = CI-published product; humans never hand-commit to `main` (ADR 0014). The `_meta/plans/` planning desk does **not** carry (already migrated to the executive desk). Claude-Code-baseline only — cross-vendor formats deferred per charter G1 (re-triage ruling on #59).

**Shipping bundles (exactly two, self-authored):**

- **project-workflow** — the session-discipline loop: handoff, planning-desk, comms, github-project-board, board-triage + the context-watermark and handoff-freshness hooks as its nudge layer.
- **repo-standards** — repo-compliance-audit, mise-en-place-scaffold, memory-taxonomy, repo-meta-structure, readme-value-and-proof, private-fork.

**Also ships:** one-skill distribution — port the reviewed skill-bundle reference banked on `dev` via #116. A rebuild **requirement**, not an option (0016).

**Retired:** deep-agents, langchain-stack, frontend-extras, meta, openspec → `externals.yaml` by reference (build-time pull mechanism = #36, native sub-issue; physical moves are **not** a blocker — record intent now). obsidian-plugin-dev demotes to candidate (ledger on the executive desk).

## Binding constraints (carry unchanged — do not relitigate)

- **Composition principle (0007):** never pass a third party's bundle along wholesale; compose bundles from third-party + authored primitives; third-party pulled at build time; vendor only unmaintained/drifted items.
- **ADR 0015:** `primitives-core` self-authored only; third-party by reference in `externals.yaml`. Agent-generated work under direction counts as self-authored (desk decision 0005).
- **Packaging P1–P4 (0006):** bundles map to workflows only; distribution via plugin only; every plugin folder ships a README with honest value/proof; encouraged — one workflow-aware agent + one nudge hook per plugin.
- **Identity neutrality (0013 item 9, via the entry gate):** no name/org/repo/issue hardcoded in shipped artifacts; personalization only via data surfaces.
- **Hook layout:** the ratified hook-dir layout (`hooks/<name>/hook.py`). The entry gate **accepts** it — the old H4 fork is resolved.

## Deliverables

- **D1 — fresh-history base:** new root history on `dev`; repo scaffold with a Makefile task interface (`make ci`), and regen scripts + drift guards for anything generated.
- **D2 — project-workflow bundle** (contents above; the two hooks promoted in as its nudge layer).
- **D3 — repo-standards bundle.**
- **D4 — one-skill distribution,** ported from the #116 reference implementation.
- **D5 — `externals.yaml`:** every retired stack recorded by reference (upstream URL + ref intent); physical clone-at-build is #36's scope.
- **D6 — entry-gate machine floor** as required CI on every PR into `dev` (contract: the executive desk's `distribution-entry-gate.md` spec):
  1. **identity-neutrality lint** — seed from the archived workbench's `scripts/promote_check.py`;
  2. **tests pass**;
  3. **provenance/externals conformance** — no third-party copies in `primitives-core`; externals entries by reference with recorded intent;
  4. **hook-layout check** — hook-dir layout accepted/enforced.
- **D7 — `docs/CONTRIBUTING.md`** carrying the extender-development SOP (graduates from the executive desk), plus the in-repo ADR mirrors (dev/main rule ADR carried from #116's PR).
- **D8 — READMEs + honest descriptions** for both bundles and the marketplace root (P3).

## Acceptance criteria

- Marketplace manifest lists exactly the two 0007 bundles (plus one-skill entries per D4); no third-party stacks ship.
- `make ci` green, and every D6 check is **demonstrably red-able** — each fails on a seeded violation, not vacuously green.
- The machine-floor checks are required status checks on PRs into `dev`.
- No hand-edited generated artifacts: each has a regen script and a drift guard that fails on divergence.
- `primitives-core` contains no third-party copies; every `externals.yaml` entry has non-null recorded intent or is dropped with a note.
- **Install smoke proof:** both bundles install and their skills load in a fresh Claude Code session (observed, not asserted); one-skill install proven for at least one skill.
- Fresh history on the same remote; `project-workflow@dotfiles-agents` still resolves by name.
- `docs/CONTRIBUTING.md` + ADR mirrors present.

## Cutover (after the rebuild is green; each step owner-gated)

1. `main` rebuilt **exactly once** by the CI publish workflow from `dev` — never by hand.
2. Branch protections: machine floor required on `dev` PRs; `main` publish-only.
3. Marketplace cache re-sync on the machine: **remove + re-add** (the local clone pinned at `452969a` cannot `git pull` across unrelated history).
4. Verify installed plugins resolve post-cutover (`enabledPlugins` is name-keyed — pre-verified).

## Relations & sequencing

- **#36** — native sub-issue (clone-at-build externals); not a hard blocker for this epic.
- **#32** — sequenced after this epic (its targets get renamed here; map once, not twice).
- **#112/#113/#114** — absorbed: their content is D2/D3, D5, and D8; closed with pointers to this body.
- Candidates (foreman-kit, python fold, curate-memories, the deferred 10-hook eval) enter via entry-gate PRs **after** the rebuild; the candidate ledger lives on the executive desk.

## Out of scope

- Cross-vendor / agnostic base formats (#59 deferred per charter G1).
- Planning-desk content of any kind (lives on the executive desk).
- Physical third-party clone-at-build mechanics (#36).


## Comments (7)

### Comment by @hsb3 at 2026-07-14T14:11:06Z

Owner ruling 2026-07-14 (executive-desk `decisions/0010`): `dev` now exists — cut from `main` at `452969a`. R1–R3 (#112–#114) have a real target branch: branch off `dev`, PR into `dev`, never `main`. The in-repo mirror ADR for the dev/main rule (old-desk ADR 0014) is being authored via #115's first PR; the `main` rebuild still waits for this epic per the cutover plan.

### Comment by @hsb3 at 2026-07-16T20:58:17Z

Reframed by decision 0016: the vehicle for this epic changes from an in-place rewrite to a CLEAN-ROOM rebuild of `dotfiles-agents` (same repo name, fresh history) directly to the 0007 lineup. Sub-issues #112/#113/#114 are absorbed as the content of that rebuild (not moot). The dev/main cutover end-state is absorbed by construction. skill-bundle #116 is merged to dev and banked as the rebuild reference. New: the distribution entry gate (tiered machine-floor CI + judgment review) folds into this rebuild's acceptance criteria.

### Comment by @hsb3 at 2026-07-17T03:08:12Z

Body replaced 2026-07-16 per decision 0016 (executive desk): reframed from in-place rewrite to clean-room rebuild work order — fresh history, same name/remote, entry-gate machine floor folded in as D6 + acceptance criteria, #116 skill-bundle as the porting reference, cutover checklist appended. Prior body in edit history. Rebuild go + dispatch mode remain the owner's call.

### Comment by @hsb3 at 2026-07-17T04:18:25Z

Dispatch prep complete (2026-07-17, executive desk): this epic now has a Multica execution mirror — issue DEV-33 in the "dotfiles-agents rebuild" project (dev-tooling workspace), assigned to foreman-claude, parked in backlog. GH #36 mirrors as DEV-34 (staged sub-issue). This GitHub body remains the source of record; Multica drives crew execution and status mirrors back via the GitHub app. Dispatch = flipping DEV-33 to todo (owner's go).

### Comment by @hsb3 at 2026-07-17T13:40:41Z

Owner gate 1 cleared (2026-07-17, desk-executed with Henry's explicit authorization): `dev` reset to the D1 orphan root `c8ea8a0` (verified parents:0); the full old source history is preserved at `dev-legacy` (`a0dae18`); `main` untouched at `452969a`. D2–D8 now proceed as normal PRs into `dev` per ADR 0014. Crew resumed on Multica DEV-33 (Wave 1: bundle skill ports). Outstanding owner rulings: R2 (standalone catalog scope, gates D4/D8) and J5 (externals keep/drop, feeds D5).

### Comment by @hsb3 at 2026-07-20T13:00:56Z

## ✅ Install-smoke proof — the last unrecorded acceptance item

Acceptance criterion: _"both bundles install and their skills load in a fresh Claude Code session (observed, not asserted); one-skill install proven for at least one skill."_ Recording it here against the shipped **0020 lineup** (#134 superseded 0007's two-bundle target with the 10-plugin desk-set).

**Method — observed, not asserted.** Each plugin was loaded from its generated on-disk artifact (`plugins/<name>/`) into a **fresh** headless session (`claude --print --output-format stream-json`). The harness's own `init` event enumerates the session's loaded `skills`/`agents` — a deterministic, non-model manifest of what actually loaded. Each session also completed a real turn (result `success`, output `OK`), so this is load **and** function, not just registration.

### Fresh-session observations

**Session A** — `cwd = repo`, the four config-inactive plugins loaded purely via `--plugin-dir`. `init` reported **9/10** loaded (all skills namespaced by plugin):

| Plugin | Kind | Skills loaded | Agents |
|---|---|---|---|
| diagrams | standalone | diagrams, drawio, excalidraw, mermaid (4) | — |
| exec-desk | desk bundle | board-triage, comms, planning-desk, pptx-themes (4) | — |
| foreman-kit | desk bundle | foreman, handoff (2) | builder, lead, reviewer, scout (4) |
| github-project-board | standalone | github-project-board (1) | — |
| obsidian-toolkit | standalone | obsidian-api-basics, obsidian-chat-ui, obsidian-cli, obsidian-mcp-server (4) | — |
| opencode-expertise | standalone | opencode-expertise (1) | — |
| owner-signoff | standalone | owner-signoff (1) | — |
| pptx-themes | standalone | pptx-themes (1) | — |
| private-fork | standalone | private-fork (1) | — |

> Note: `diagrams` + `private-fork` were `disabled` and `pptx-themes` + `obsidian-toolkit` were `not-installed` in this machine's config — they loaded **from the tracked artifact** via `--plugin-dir`, which is exactly the "install into a fresh session and watch it load" proof.

**Session B** — `cwd = neutral` (no project scope), `--plugin-dir plugins/code-desk`. `init` reported **code-desk** loading all 7 skills: memory-taxonomy, mise-en-place-scaffold, opencode-expertise, private-fork, readme-value-and-proof, repo-compliance-audit, repo-meta-structure.

**Union = 10/10 lineup plugins observed loading from their generated artifacts.**

### Cross-checks

- **Live interactive session:** this session (running against `dotfiles-agents@main`) has code-desk, exec-desk, foreman-kit, github-project-board, opencode-expertise, owner-signoff loaded and usable; foreman-kit's four agent types are dispatchable.
- **One-skill install (criterion, explicit):** the 5 one-skill standalone plugins — github-project-board, opencode-expertise, owner-signoff, pptx-themes, private-fork — each loaded their single skill. Comfortably exceeds "at least one." `make catalog` confirms all 5 wrappers byte-identical to source.
- **Gate run locally (not asserted):** `make ci` **green** — 120 tests; roster↔disk clean (31 primitives), identity-neutral, provenance clean, hook-layout clean, skill-catalog + standalone-wrapper invariants OK, marketplace artifacts match source (no drift).
- **State:** `dev` and `main` level at `a6c77e4`; marketplace resolves by name (`code-desk@dotfiles-agents` et al. install and enable).

All acceptance criteria for this epic are now met. Closing.


### Comment by @hsb3 at 2026-07-20T13:01:17Z

Closing — all acceptance criteria met. Install-smoke proof recorded above (10/10 lineup plugins observed loading from generated artifacts in fresh sessions; make ci green, 120 tests; dev/main level at a6c77e4). Follow-ons that were never blockers for this epic remain tracked separately: #36 clone-at-build externals, #32 hsb3-custom-plugins retirement.
