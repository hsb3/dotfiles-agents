# _meta/_archive — retired working material

Frozen, no-longer-active desk material kept for provenance: snapshots taken before a
reset, and build-era handoffs whose wave has closed. Nothing here is a live reference —
active investigations live in `research/`, current planning in `plans/`. Contents are
read-only history; append new archive subdirs, never edit the ones already here.
